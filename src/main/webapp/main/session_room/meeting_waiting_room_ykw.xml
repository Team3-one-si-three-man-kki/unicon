<html xmlns="http://www.w3.org/1999/xhtml" xmlns:ev="http://www.w3.org/2001/xml-events" xmlns:w2="http://www.inswave.com/websquare"
	xmlns:xf="http://www.w3.org/2002/xforms">
	<head>
		<w2:type>COMPONENT</w2:type>
		<w2:buildDate />
		<w2:MSA />
		<xf:model>
			<w2:dataCollection baseNode="map">
				<w2:aliasDataMap studioSrc="/InsWebApp/ui/session_room/session_room3.xml" scope="../meetingSettings"
					id="alias_meetingSettings"></w2:aliasDataMap>
			</w2:dataCollection>
			<w2:workflowCollection />
		</xf:model>
		<w2:layoutInfo />
		<w2:publicInfo method="" />
		<script lazy="false" type="text/javascript"><![CDATA[scwin.onpageload = function () {
    scwin.meetInfo = alias_meetingSettings.getObj();
    scwin.initCamera();
    scwin.initLocalState();
};

// MediaStream 전역 보관 변수
scwin.mediaStream = null;
scwin.currentSessionId = $p.parent().scwin.currentSessionId;

// 로컬 상태 저장 변수
scwin.localState = {
    videoEnabled: true,
    audioEnabled: true,
    participantName: "",
    participantEmail: ""
};

// 1) 카메라·마이크 초기 스트림 요청
scwin.initCamera = function () {
    navigator.mediaDevices.getUserMedia({ video: true, audio: true })
        .then(function (stream) {
            scwin.mediaStream = stream;
            var webcamElement = document.getElementById("webcam");
            if (webcamElement) {
                webcamElement.srcObject = stream;
            }
        })
        .catch(function (err) {
            alert("카메라/마이크 접근에 실패했습니다.");
        });
};

// 2) 로컬 상태 초기화 및 버튼 스타일 설정
scwin.initLocalState = function () {

    // DOM 준비 대기 후 스타일 적용
    setTimeout(function () {
        scwin.updateButtonStyle("video", scwin.localState.videoEnabled);
        scwin.updateButtonStyle("audio", scwin.localState.audioEnabled);
    });

    // 초기 미디어 트랙 상태 설정
    scwin.toggleTracks("video", scwin.localState.videoEnabled);
    scwin.toggleTracks("audio", scwin.localState.audioEnabled);
};

// 3) 버튼 스타일 업데이트
scwin.updateButtonStyle = function (type, enabled) {
    var btnElement;
    var className;

    if (type === "video") {
        btnElement = document.getElementById("mf_waiting_room_frame_btn_video");
        className = "btn_video";
    } else if (type === "audio") {
        btnElement = document.getElementById("mf_waiting_room_frame_btn_mic");
        className = "btn_mic";
    }

    // DOM 요소가 없으면 재시도  -> session_room.xml에 띄우니까 로드시간 차이가 생겨서 이미지가 직접 안들어가는 문제 발생
    if (!btnElement) {
        setTimeout(function () {
            scwin.updateButtonStyle(type, enabled);
        }, 100);
        return;
    }

    if (btnElement) {
        // 기존 on/off 클래스 제거
        btnElement.classList.remove("on", "off");
        // 새로운 상태 클래스 추가
        btnElement.classList.add(enabled ? "on" : "off");
        // 기본 클래스도 유지
        if (!btnElement.classList.contains(className)) {
            btnElement.classList.add(className);
        }
        // data-state 속성 업데이트
        btnElement.setAttribute("data-state", enabled ? "on" : "off");
    }
};

// 4) 미디어 트랙 활성 설정
scwin.toggleTracks = function (type, enabled) {
    if (!scwin.mediaStream) return;

    if (type === "video") {
        var videoTracks = scwin.mediaStream.getVideoTracks();
        videoTracks.forEach(function (track) {
            track.enabled = enabled;
        });
    } else if (type === "audio") {
        var audioTracks = scwin.mediaStream.getAudioTracks();
        audioTracks.forEach(function (track) {
            track.enabled = enabled;
        });
    }
};

// 5) 비디오 버튼 클릭 이벤트 (실시간 데이터맵 저장)
scwin.onVideoButtonClick = function () {
    // 상태 토글
    scwin.localState.videoEnabled = !scwin.localState.videoEnabled;

    // 스타일 업데이트
    scwin.updateButtonStyle("video", scwin.localState.videoEnabled);

    // 비디오 트랙 토글
    scwin.toggleTracks("video", scwin.localState.videoEnabled);

    // 실시간 데이터맵 저장
    scwin.saveStateToDataMap();

    console.log("비디오 상태 변경:", scwin.localState.videoEnabled ? "true" : "false");
};


// 6) 오디오 버튼 클릭 이벤트 (실시간 데이터맵 저장)
scwin.onAudioButtonClick = function () {
    // 상태 토글
    scwin.localState.audioEnabled = !scwin.localState.audioEnabled;

    // 스타일 업데이트
    scwin.updateButtonStyle("audio", scwin.localState.audioEnabled);

    // 오디오 트랙 토글
    scwin.toggleTracks("audio", scwin.localState.audioEnabled);

    // 실시간 데이터맵 저장
    scwin.saveStateToDataMap();

    console.log("오디오 상태 변경:", scwin.localState.audioEnabled ? "true" : "false");
};


// 7) 입장하기 버튼 클릭 이벤트
scwin.onEnterMeeting = function () {
    var nameElement = $p.getComponentById("participant_name");
    var emailElement = $p.getComponentById("participant_email");

    if (!nameElement || !emailElement) {
        alert("입력 필드를 찾을 수 없습니다.");
        return;
    }

    var name = nameElement.getValue().trim();
    var email = emailElement.getValue().trim();

    if (!name) {
        alert("참여자 이름을 입력해주세요.");
        nameElement.focus();
        return;
    }

    if (!email) {
        alert("이메일을 입력해주세요.");
        emailElement.focus();
        return;
    }

    // 이메일 형식 간단 검증
    var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(email)) {
        alert("올바른 이메일 형식을 입력해주세요.");
        emailElement.focus();
        return;
    }

    // 로컬 상태에 입력값 저장
    scwin.localState.participantName = name;
    scwin.localState.participantEmail = email;

    // 최종 데이터맵에 상태 저장
    scwin.saveToDataMap();

    console.log("입장 정보 저장 완료:", scwin.localState);

    // 부모 페이지의 대기방 숨기기 함수 호출
    $p.parent().scwin.hideWaitingRoom();
};


// 8) 실시간 상태만 데이터맵에 저장 (새로운 함수)
scwin.saveStateToDataMap = function () {
    try {
        // AliasDataMap에 video/audio 상태만 저장
        if (typeof alias_meetingSettings !== 'undefined' &&
            alias_meetingSettings &&
            typeof alias_meetingSettings.set === 'function') {

            alias_meetingSettings.set("videoEnabled", scwin.localState.videoEnabled ? "true" : "false");
            alias_meetingSettings.set("audioEnabled", scwin.localState.audioEnabled ? "true" : "false");
        }
    } catch (e) {
        console.error("상태 저장 오류:", e.message);
    }
};


// 9) 전체 데이터맵에 저장 (기존 로직 개선)
scwin.saveToDataMap = function () {
    console.log("saveToDataMap 진입");
    try {

        console.log("meetInfo:", scwin.meetInfo);
        // AliasDataMap에 저장 시도

        console.log("데이터맵 저장처리");

        scwin.meetInfo.set("participantName", scwin.localState.participantName);
        scwin.meetInfo.set("participantEmail", scwin.localState.participantEmail);
        scwin.meetInfo.set("videoEnabled", scwin.localState.videoEnabled ? "true" : "false");
        scwin.meetInfo.set("audioEnabled", scwin.localState.audioEnabled ? "true" : "false");

        console.log("AliasDataMap 저장 성공");

        // WebSquare localStorage 저장
        if (typeof WebSquare !== 'undefined' && WebSquare.localStorage) {
            WebSquare.localStorage.setItem("meetingSettings", JSON.stringify({
                participantName: scwin.localState.participantName,
                participantEmail: scwin.localState.participantEmail,
                videoEnabled: scwin.localState.videoEnabled ? "true" : "false",
                audioEnabled: scwin.localState.audioEnabled ? "true" : "false"
            }));
            console.log("WebSquare localStorage 저장 성공");
            console.log("웹스퀘어스토리지" + WebSquare.localStorage.getItem("meetingSettings"));
            return;
        }

        // 전역 변수로 저장 
        window.meetingData = {
            participantName: scwin.localState.participantName,
            participantEmail: scwin.localState.participantEmail,
            videoEnabled: scwin.localState.videoEnabled ? "true" : "false",
            audioEnabled: scwin.localState.audioEnabled ? "true" : "false"
        };

        console.log("전역 변수 저장 완료");

    } catch (e) {
        console.error("데이터맵 저장 오류:", e.message);
        alert("설정 저장에 실패했습니다.");
    }
};


// 10) 데이터맵에서 상태 로드 (새로운 함수)
scwin.loadFromDataMap = function () {
    try {
        // AliasDataMap에서 로드
        if (typeof alias_meetingSettings !== 'undefined' &&
            alias_meetingSettings &&
            typeof alias_meetingSettings.get === 'function') {

            var videoEnabled = alias_meetingSettings.get("videoEnabled");
            var audioEnabled = alias_meetingSettings.get("audioEnabled");

            if (videoEnabled) scwin.localState.videoEnabled = (videoEnabled === "on");
            if (audioEnabled) scwin.localState.audioEnabled = (audioEnabled === "on");

            console.log("AliasDataMap에서 상태 로드 완료");
        }
    } catch (e) {
        console.error("데이터맵 로드 오류:", e.message);
    }
};


// 11) 페이지 종료 시 스트림 정리 (기존 로직 유지)
scwin.onpageunload = function () {
    if (scwin.mediaStream) {
        scwin.mediaStream.getTracks().forEach(function (track) {
            track.stop();
        });
        scwin.mediaStream = null;
        console.log("미디어 스트림 정리 완료");
    }
};
]]></script>
		<style><![CDATA[
            /* 전체 페이지 스타일 */
            body {
                margin: 0;
                padding: 0;
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                background: linear-gradient(135deg, #667eea 0%, #beabd2 100%);
                height: 100vh;
                overflow: hidden;
            }

            .sub_contents {
                height: 100vh !important;
                margin: 0;
                padding: 0;
                display: flex;
                flex-direction: column;
            }

            /* 헤더 스타일 - 피그마의 상단 영역 */
            .pgtbox {
                background: rgba(255,255,255,0.1) !important;
                backdrop-filter: blur(10px);
                border: none !important;
                box-shadow: 0 2px 20px rgba(0,0,0,0.1);
                display: flex;
                align-items: center;
                justify-content: center;
                height: 80px !important;
                margin: 0;
            }

            .pgt_tit {
                color: white !important;
                font-size: 1.8rem !important;
                font-weight: bold !important;
                text-align: center;
                margin: 0;
            }

            .breadcrumb {
                display: none;
            }

            /* 메인 컨테이너 */
            .lybox {
                flex: 1;
                display: flex !important;
                align-items: center;
                justify-content: center;
                padding: 40px;
                gap: 40px;
            }

            /* 왼쪽 비디오 영역 - 피그마의 어두운 비디오 영역 */
            .col_5:first-child {
                background: rgba(45, 55, 72, 0.95) !important;
                border-radius: 20px !important;
                box-shadow: 0 10px 30px rgba(0,0,0,0.3);
                padding: 30px;
                max-width: 500px;
                height: 550px;
                display: flex !important;
                flex-direction: column !important;
                justify-content: center !important;
                backdrop-filter: blur(10px);
            }

            /* 비디오 엘리먼트 */
            #webcam {
                width: 100% !important;
                height: 300px !important;
                border-radius: 15px !important;
                background: rgba(0,0,0,0.3) !important;
                margin-bottom: 30px !important;
                object-fit: cover;
                border: 2px solid rgba(255,255,255,0.1);
            }

            /* 비디오 컨트롤 버튼 영역 */
            #webcam_video {
                display: flex !important;
                justify-content: center !important;
                gap: 20px !important;
                align-items: center !important;
            }

            /* 기본 상태 */
            .btn_video {
                background-image: url('/InsWebApp/images/icons/video_on.png') !important;
                background-color: #00C851 !important;
            }

            /* 특정 상태를 아래로 이동 (더 높은 우선순위) */
            .btn_video.off {
                background-image: url('/InsWebApp/images/icons/video_off.png') !important;
                background-color: #ff4444 !important;
            }

            /* 비디오/마이크 버튼 스타일 */
            .btn_video, .btn_mic {
                width: 60px !important;
                height: 60px !important;
                border-radius: 50% !important;
                border: none !important;
                background-size: 50% !important;
                background-repeat: no-repeat !important;
                background-position: center !important;
                cursor: pointer !important;
                transition: all 0.3s ease !important;
                box-shadow: 0 4px 15px rgba(0,0,0,0.2) !important;
                margin: 0 !important;
            }

            /* ON 상태 */
            .btn_video.on {
                background-image: url('/InsWebApp/images/icons/video_on.png') !important;
                background-color: #00C851 !important;
            }
            .btn_mic.on {
                background-image: url('/InsWebApp/images/icons/mic_on.png') !important;
                background-color: #00C851 !important;
            }

            /* OFF 상태 */
            .btn_video.off {
                background-image: url('/InsWebApp/images/icons/video_off.png') !important;
                background-color: #ff4444 !important;
            }
            .btn_mic.off {
                background-image: url('/InsWebApp/images/icons/mic_off.png') !important;
                background-color: #ff4444 !important;
            }

            .btn_video:hover, .btn_mic:hover {
                transform: translateY(-2px) !important;
                box-shadow: 0 6px 20px rgba(0,0,0,0.3) !important;
            }

            /* 오른쪽 입력 폼 영역 - 피그마의 흰색 카드 */
            .col_5:last-child {
                background: rgba(255,255,255,0.95) !important;
                border-radius: 20px !important;
                box-shadow: 0 10px 30px rgba(0,0,0,0.2);
                padding: 40px;
                max-width: 400px;
                height: 550px;
                backdrop-filter: blur(10px);
                display: flex;
                flex-direction: column;
                justify-content: flex-start;
            }

            /* ModuLink 로고 스타일 */
            #meet_title {
                color: #667eea !important;
                font-size: 20px !important;
                font-weight: bold !important;
                margin-bottom: 10px !important;
                text-align: center;
            }

            #meet_title::before {
                content: "🔗 ";
                margin-right: 8px;
            }

            /* 회의 제목 하위 설명 텍스트 */
            .col_5:last-child p {
                color: #718096 !important;
                font-size: 0.8rem !important;
                margin-bottom: 20px !important;
                text-align: center !important;
                line-height: 1.4 !important;
            }

            /* 입력 필드 라벨 */
            .titbox h6 {
                color: #4a5568 !important;
                font-size: 18px !important;
                font-weight: 600 !important;
                margin-bottom: 8px !important;
                margin-top: 20px !important;
            }

            /* 입력 필드 스타일 */
            #participant_name, #participant_email {
                width: 100% !important;
                padding: 12px 16px !important;
                border: 1px solid #e2e8f0 !important;
                border-radius: 8px !important;
                font-size: 1rem !important;
                margin-bottom: 15px !important;
                box-sizing: border-box !important;
                transition: border-color 0.3s ease !important;
            }

            #participant_name:focus, #participant_email:focus {
                outline: none !important;
                border-color: #667eea !important;
                box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1) !important;
            }

            /* 장치 상태 섹션 */
            .txt_info {
                color: #4a5568 !important;
                font-size: 0.9rem !important;
                font-weight: 600 !important;
                margin-top: 20px !important;
                margin-bottom: 10px !important;
            }

            .txt_success {
                color: #00C851 !important;
                font-size: 0.8rem !important;
                margin-bottom: 5px !important;
                display: flex !important;
                align-items: center !important;
            }

            .txt_success::before {
                content: "✓ ";
                margin-right: 8px;
                font-weight: bold;
            }

            /* 입장하기 버튼 */
            .btn_cm {
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
                color: white !important;
                border: none !important;
                padding: 12px 24px !important;
                border-radius: 25px !important;
                font-size: 1rem !important;
                font-weight: 600 !important;
                cursor: pointer !important;
                transition: all 0.3s ease !important;
                margin-top: 50px !important;
                width: 100% !important;
                box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3) !important;
                height: 50px;
            }

            .btn_cm:hover {
                transform: translateY(-2px) !important;
                box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4) !important;
            }

            .btn_cm::before {
                content: "🚀 ";
                margin-right: 8px;
            }

            /* 반응형 디자인 */
            @media (max-width: 768px) {
                .lybox {
                    flex-direction: column !important;
                    padding: 20px !important;
                    gap: 20px !important;
                }

                .col_5:first-child, .col_5:last-child {
                    max-width: 100% !important;
                    height: auto !important;
                }

                #webcam {
                    height: 200px !important;
                }
            }

            /* 애니메이션 효과 */
            @keyframes fadeInUp {
                from {
                    opacity: 0;
                    transform: translateY(30px);
                }
                to {
                    opacity: 1;
                    transform: translateY(0);
                }
            }

            .col_5 {
                animation: fadeInUp 0.6s ease-out;
            }

            .col_5:last-child {
                animation-delay: 0.2s;
            }
         ]]></style>
	</head>
	<body ev:onpageload="scwin.onpageload">
		<xf:group class="sub_contents" id="" meta_snippetCategory="00_화면시작" meta_snippetKeyComponent="true" meta_snippetName="0_01 페이지시작"
			style="height: 100%;">
			<xf:group meta_snippetCategory="02_타이틀" meta_snippetName="2_03 서브타이틀(h4)" meta_snippetKeyComponent="true" style="" id=""
				class="titbox">
				<xf:group class="pgtbox" id="" meta_snippetCategory="02_타이틀" meta_snippetKeyComponent="true" meta_snippetName="2_01 페이지타이틀"
					style="background-color: #008080;height: 70px;">
					<w2:textbox class="pgt_tit" id="" label="Unicon, 소통의 새로운 시작" style="" tagname=""></w2:textbox>
					<xf:group class="breadcrumb" id="" style="">
						<xf:group id="" style="" tagname="ul">
							<xf:group id="" style="" tagname="li">
							</xf:group>
						</xf:group>
					</xf:group>
				</xf:group>
			</xf:group>
			<xf:group class="lybox" id="" meta_snippetCategory="01_화면분할" meta_snippetKeyComponent="true" meta_snippetName="1_01 가변 2단 분할"
				style="height: 100%;">
				<xf:group adaptiveThreshold="" class=" col_5" id="" style="display: flex; flex-direction: column; justify-content: center;">
					<video style="width: 100%; height: 50%;margin-bottom: 20px;" id="webcam" autoplay="autoplay" muted="muted"></video>
					<xf:group id="webcam_video" style="display: flex;align-items: center;justify-content: center;gap: 20px">
						<xf:trigger style="margin: auto;" id="btn_video" type="button" data-state="on" class="btn_video"
							ev:onclick="scwin.onVideoButtonClick">
							<xf:label><![CDATA[]]>
							</xf:label>
						</xf:trigger>
						<xf:trigger id="btn_mic" style="margin: auto;" type="button" data-state="on" class="btn_mic"
							ev:onclick="scwin.onAudioButtonClick">
							<xf:label><![CDATA[]]>
							</xf:label>
						</xf:trigger>
					</xf:group>
				</xf:group>
				<xf:group class=" col_5" id="" style="">
					<xf:group class="titbox" id="" meta_snippetCategory="02_타이틀" meta_snippetKeyComponent="true" meta_snippetName="2_03 서브타이틀(h4)"
						style="">
						<w2:textbox class="" id="meet_title" label="세션 입장 정보" style="" tagname="h6"></w2:textbox>
					</xf:group>
					<xf:group class="titbox" id="" meta_snippetCategory="02_타이틀" meta_snippetKeyComponent="true" meta_snippetName="2_03 서브타이틀(h4)"
						style="">
						<w2:textbox class="" id="" label="참여자 이름 *" style="" tagname="h6"></w2:textbox>
					</xf:group>
					<xf:input class="" id="participant_name" meta_snippetCategory="10_입력폼" meta_snippetKeyComponent="true"
						meta_snippetName="10_01 InputBox" placeholder="이름을 입력하세요" style="width: 250px;">
					</xf:input>
					<xf:group class="titbox" id="" meta_snippetCategory="02_타이틀" meta_snippetKeyComponent="true" meta_snippetName="2_03 서브타이틀(h4)"
						style="">
						<w2:textbox class="" id="" label="이메일" style="" tagname="h6"></w2:textbox>
					</xf:group>
					<xf:input class="" id="participant_email" meta_snippetCategory="10_입력폼" meta_snippetKeyComponent="true"
						meta_snippetName="10_01 InputBox" placeholder="이메일을 입력하세요" style="width: 250px;margin-bottom: 20px">
					</xf:input>
					<w2:textbox class="txt_info" dataType="" id="" label="장치 상태 확인" meta_snippetCategory="11_메시지"
						meta_snippetKeyComponent="true" meta_snippetName="11_03 정보제공메시지" style="">
					</w2:textbox>
					<w2:textbox class="txt_success" dataType="" id="" label="마이크" meta_snippetCategory="11_메시지" meta_snippetKeyComponent="true"
						meta_snippetName="11_02 정상처리메시지" style="">
					</w2:textbox>
					<w2:textbox class="txt_success" dataType="" id="" label="카메라" meta_snippetCategory="11_메시지" meta_snippetKeyComponent="true"
						meta_snippetName="11_02 정상처리메시지" style="">
					</w2:textbox>
					<xf:trigger class="btn_cm" id="" meta_snippetCategory="08_버튼" meta_snippetKeyComponent="true" meta_snippetName="8_01 기본버튼"
						style="" type="button" ev:onclick="scwin.onEnterMeeting">
						<xf:label><![CDATA[입장하기]]></xf:label>
					</xf:trigger>
				</xf:group>
			</xf:group>
		</xf:group>
	</body>
</html> 